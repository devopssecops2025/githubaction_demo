name : Hello Word Workflow
on: 
  push:
    branches:
      - main
jobs:
  hello:
     runs-on: ubuntu-latest
     steps:
       - name: Hello Step
         run: echo "My name is ${{vars.My_Name}}"
 
    
